# Backup and Recovery

Backups are JSON files and remain local to the user. Imports are validated before replacing data, and failed validation preserves current data. Recovery snapshots are bounded to three local copies and are created before supported destructive operations.
